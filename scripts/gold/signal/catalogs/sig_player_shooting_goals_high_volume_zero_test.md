---
signal_id: sig_player_shooting_goals_high_volume_zero_test
status: active
entity: player
family: shooting
subfamily: goals
grain: match_player
headline: "High-Volume Zero Test"
trigger: "Player takes >= 5 shots, all off-target (wide/over), in a single finished match."
row_identity:
  - match_id
  - triggered_player_id
  - triggered_team_id
asset_paths:
  table: gold_signals.sig_player_shooting_goals_high_volume_zero_test
  sql: clickhouse/gold/dml/signals/player/sig_player_shooting_goals_high_volume_zero_test.sql
  runner: scripts/gold/run_gold_sql_jobs.py
---
# sig_player_shooting_goals_high_volume_zero_test

## Purpose

Flags high-volume, completely off-frame shooting matches (`total_shots >= 5`, `shots_on_target = 0`, `blocked_shots = 0`) to isolate players whose shot selection and execution fail to test the goalkeeper at all.

## Tactical And Statistical Logic

- Trigger condition:
  - `triggered_player_total_shots >= 5`
  - `triggered_player_shots_on_target = 0`
  - `triggered_player_blocked_shots = 0`
  - `triggered_player_shots_off_target = triggered_player_total_shots`
- Triggered player identity and shooting metrics come from `silver.player_match_stat` in finished matches only.
- Bilateral team context is sourced from `silver.period_stat` (`period = 'All'`) with symmetric `triggered_team_*` and `opponent_*` features for shots, off-target profile, chance quality, possession, and box touches.
- Signal adds severity diagnostics (`shots_above_threshold`, `triggered_player_shots_off_target_pct`, `expected_goals_wasted`) to rank how extreme each all-off-target output is.
- Similarity gate note: closest active signals are `sig_player_shooting_goals_shot_volume_monster`, `sig_player_shooting_goals_impact_accumulator`, and `sig_player_shooting_goals_wasteful_finisher`. This signal coexists by requiring a strict off-target-only shot profile, which those signals do not enforce.

## Technical Assets

- SQL: `clickhouse/gold/dml/signals/player/sig_player_shooting_goals_high_volume_zero_test.sql`
- Runner: `scripts/gold/run_gold_sql_jobs.py`
- Target table: `gold_signals.sig_player_shooting_goals_high_volume_zero_test`

## Example Execution

```bash
python3 scripts/gold/run_gold_sql_jobs.py --date YYYYMMDD --kind signal --id sig_player_shooting_goals_high_volume_zero_test
```

## Output Schema

| Column Name | Description | Reason |
|---|---|---|
| `match_id` | Unique match identifier | Stable join and deduplication key |
| `match_date` | Match date | Time slicing for scouting and trend analysis |
| `home_team_id` | Home team identifier | Match context anchor |
| `home_team_name` | Home team name | Readable context |
| `away_team_id` | Away team identifier | Match context anchor |
| `away_team_name` | Away team name | Readable context |
| `home_score` | Home full-time score | Scoreline context for interpreting wasted shooting |
| `away_score` | Away full-time score | Scoreline context for interpreting wasted shooting |
| `triggered_side` | Triggered player side (`home`/`away`) | Canonical side orientation |
| `triggered_player_id` | Triggered player identifier | Player identity key |
| `triggered_player_name` | Triggered player name | Human-readable player attribution |
| `triggered_team_id` | Triggered player team identifier | Team linkage for player event |
| `triggered_team_name` | Triggered player team name | Human-readable team attribution |
| `opponent_team_id` | Opponent team identifier | Opponent context linkage |
| `opponent_team_name` | Opponent team name | Human-readable opponent attribution |
| `trigger_threshold_min_total_shots` | Minimum shot-volume trigger threshold (`5`) | Trigger auditability |
| `trigger_threshold_max_shots_on_target` | Maximum shots-on-target trigger threshold (`0`) | Trigger auditability |
| `trigger_threshold_max_blocked_shots` | Maximum blocked-shots trigger threshold (`0`) | Trigger auditability |
| `triggered_player_goals` | Goals scored by triggered player | Outcome context against high volume |
| `triggered_player_expected_goals` | Expected goals generated by triggered player | Chance-quality context for misses |
| `triggered_player_total_shots` | Total shots attempted by triggered player | Primary volume trigger metric |
| `triggered_player_shots_on_target` | Triggered player shots on target | Verifies zero-test condition |
| `triggered_player_shots_off_target` | Triggered player shots off target | Core off-frame trigger component |
| `triggered_player_blocked_shots` | Triggered player blocked shots | Ensures off-target-only profile |
| `triggered_player_shot_accuracy_pct` | On-target share of player shots (%) | Precision diagnostic |
| `triggered_player_shots_off_target_pct` | Off-target share of player shots (%) | Severity diagnostic for off-frame shooting |
| `triggered_player_expected_goals_per_shot` | Expected goals per player shot | Shot-quality per-attempt context |
| `triggered_player_goal_minus_expected_goals` | Player goals minus player expected goals | Finishing over/underperformance metric |
| `triggered_player_minutes_played` | Minutes played by triggered player | Exposure context for interpretation |
| `shots_above_threshold` | Margin above trigger floor (`total_shots - 5`) | Severity ranking beyond binary trigger |
| `expected_goals_wasted` | Expected goals not converted (`expected_goals - goals`) | Missed-value magnitude |
| `triggered_player_shots_without_goal` | Non-converted player shots | Volume of unsuccessful attempts |
| `triggered_team_goals` | Goals by triggered player's team | Team finishing context |
| `opponent_goals` | Goals by opponent team | Bilateral scoreline comparator |
| `goal_delta` | Triggered-team goals minus opponent goals | Match outcome context |
| `triggered_team_expected_goals` | Triggered-side team expected goals | Team chance-quality baseline |
| `opponent_expected_goals` | Opponent team expected goals | Bilateral chance-quality comparator |
| `expected_goals_delta` | Triggered-team expected goals minus opponent | Side-level chance-quality balance |
| `triggered_team_total_shots` | Total shots by triggered side | Team shot-volume baseline |
| `opponent_total_shots` | Total shots by opponent side | Bilateral volume comparator |
| `triggered_team_shots_on_target` | Shots on target by triggered side | Team shot-execution context |
| `opponent_shots_on_target` | Shots on target by opponent side | Bilateral shot-execution comparator |
| `triggered_team_shots_off_target` | Off-target shots by triggered side | Team off-frame profile context |
| `opponent_shots_off_target` | Off-target shots by opponent side | Bilateral off-frame comparator |
| `triggered_team_blocked_shots` | Blocked shots by triggered side | Team shot suppression context |
| `opponent_blocked_shots` | Blocked shots by opponent side | Bilateral shot suppression comparator |
| `triggered_team_big_chances` | Big chances created by triggered side | High-quality chance context |
| `opponent_big_chances` | Big chances created by opponent side | Bilateral high-quality chance comparator |
| `triggered_team_possession_pct` | Triggered-side possession (%) | Control-profile context |
| `opponent_possession_pct` | Opponent-side possession (%) | Bilateral control comparator |
| `triggered_team_touches_opposition_box` | Triggered-side touches in opposition box | Territorial penetration context |
| `opponent_touches_opposition_box` | Opponent-side touches in opposition box | Bilateral territorial comparator |
| `player_share_of_team_expected_goals_pct` | Player share of team expected goals (%) | Concentration of chance-quality responsibility |
| `player_share_of_team_total_shots_pct` | Player share of team total shots (%) | Concentration of shot-volume responsibility |
| `player_share_of_team_shots_off_target_pct` | Player share of team off-target shots (%) | Concentration of off-target burden |
